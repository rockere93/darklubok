import { viewport } from "../DOM-elements/mainelements";
import { textCard } from "../DOM-elements/mainelements";

function scrollTextDown () {
        textCard.scrollTop = 1000;
};

export default scrollTextDown